# Local post-create compliance simulator

Pure in-memory Phase-A simulator. It has no GitHub credentials or network calls.
It proves trusted creation promotes only after quarantine, generated SHA-pinned
contract seed, parity attestation, and durable ruleset/property installation;
out-of-band repositories remain quarantined.
